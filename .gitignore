# macOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
.pytest_cache/

# Virtual environments
.venv/
venv/
ENV/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Coverage artifacts (all regenerated by CI; the coverage badge lives on the
# dedicated `badges` branch, not here)
.coverage
coverage.json
htmlcov/

# Description-evaluation run artifacts (ephemeral; never committed, never
# placed under tests/skill-corpus/). Emitted by evaluate_descriptions.py's
# --emit-tasks / --emit-heuristic-predictions and filled by the host agent.
*.tasks.json
*.predictions.json

# Environment variables
.env
.env.local

# Claude Code internal state (per-checkout, never shared)
.claude/scheduled_tasks.lock
